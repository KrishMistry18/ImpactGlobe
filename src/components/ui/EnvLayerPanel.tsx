"use client";
// EnvLayerPanel is now embedded inside FilterBar as a "Layers" dropdown.
// This file is kept as a no-op export so existing imports don't break.
export function EnvLayerPanel() {
  return null;
}
